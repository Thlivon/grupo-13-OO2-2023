spring:
  datasource:
    url: jdbc:mysql://localhost:3306/smartcity_unla
    username: root
    password: cecims
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      dialect: org.hibernate.dialect.MySQL5Dialect
  thymeleaf:
    cache: false